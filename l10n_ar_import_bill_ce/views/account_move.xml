<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_move_list_bill_import" model="ir.ui.view">
        <field name="name">account.move.list.bill.import</field>
        <field name="model">account.move</field>
        <field name="arch" type="xml">
            <tree string="Facturas de Proveedor Importadas" decoration-info="state == 'draft'" decoration-muted="state == 'cancel'" >
                <field name="name" string="Number"/>
                <field name="partner_id" string="Vendor"/>
                <field name="invoice_date" string="Bill Date"/>
                <field name="payment_state" optional="hide"/>
                <field name="move_type" optional="hide"/>
                <field name="invoice_date_due" string="Due Date"  widget="remaining_days" optional="show" invisible="payment_state in ('paid', 'in_payment', 'reversed') or state == 'cancel'"/>
                <field name="amount_untaxed" string="Tax Excluded"/>
                <field name="amount_total" string="Total"/>
                <field name="state" string="Status" widget="badge" decoration-info="state == 'draft'" decoration-danger="payment_state == 'not_paid' and invoice_date_due &lt; context_today().strftime('%Y-%m-%d')" decoration-warning="payment_state in ('partial', 'in_payment')" decoration-success="payment_state in ('paid', 'reversed')" invisible="payment_state == 'invoicing_legacy' or move_type == 'entry'" />
            </tree>
        </field>
    </record>
</odoo>

