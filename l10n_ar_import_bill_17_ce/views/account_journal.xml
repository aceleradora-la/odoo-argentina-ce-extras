<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_journal_form" model="ir.ui.view">
        <field name="name">account.journal.form.import.bill</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button 
                    name="action_import_bills_from_xls" 
                    string="Importar Facturas desde Excel" 
                    type="object"
                    class="btn-primary"
                    invisible="not (type == 'purchase' or (type == 'sale' and not l10n_ar_is_pos)) or company_id.country_code != 'AR' or company_id.l10n_ar_afip_responsibility_type_id.code != '1'"
                    help="Importar facturas desde archivo Excel exportado desde ARCA/AFIP"/>
            </xpath>
        </field>
    </record>
</odoo>

